const initialState = {
  title: ['title1', 'title2', 'title3', 'title4']
}

const board = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default board;