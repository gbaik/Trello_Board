/*
import { INSERT_FUNCTION_NAME } from './actions';

export default function reduce(state = {}, action) {
  switch (action.type) {
    case INSERT_DISPATCH_ACTION_NAME:
      return action.payload.data
    default:
      return state;
  }
}
*/